<Window x:Class="HotelProgram.Views.TransactionList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelProgram.Views"
        xmlns:viewModel="clr-namespace:HotelProgram.ViewModels" 
        xmlns:customcontrols="clr-namespace:HotelProgram.CustomControls"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        Title="TransactionList" Height="950" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        MouseDown="Window_MouseDown">


    <Window.Resources>
        <!-- Стиль для выравнивания заголовков столбцов по центру -->
        <Style x:Key="CenterAlignedHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12">
        <Border CornerRadius="10"
            BorderThickness="1">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                    <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.5"/>
                    <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Border.Background>
                <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                    <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0"/>
                    <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0.75"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="5"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Журнал транзакций"
                               Foreground="#D3D3D3"
                               FontSize="11"
                               FontFamily="Montserrat"
                               Grid.Column="0"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>

                    <Button x:Name="btnMinimize"
                            Style="{StaticResource loginButton}"
                            Tag="{StaticResource colorIconButtonMinimaze}"
                            Cursor="Hand"
                            Grid.Column="1"
                            Click="btnMinimize_Click">
                        <fa:IconImage Icon="WindowMinimize" Width="12" Style="{StaticResource loginButtonIcon}"/>
                    </Button>

                    <Button x:Name="btnClose"
                            Style="{StaticResource loginButton}"
                            Tag="{StaticResource colorIconButtonCLose}"
                            Cursor="Hand"
                            Grid.Column="2"
                            Click="btnClose_Click">
                        <fa:IconImage Icon="Xmark" Style="{StaticResource loginButtonIcon}"/>
                    </Button>
                </Grid>
                <Grid Grid.Row="1">
                    <DataGrid x:Name="dataGridTransaction"
                              Margin="10"
                              RowStyle="{DynamicResource DataGridRowStyle1}" 
                              ColumnHeaderStyle="{StaticResource CenterAlignedHeaderStyle}"
                              Style="{DynamicResource DataGridStyle1}"
                              CellStyle="{StaticResource Body_Content_DataGrid_Centering}"
                              IsReadOnly="True"
                              Grid.Row="0"
                              AutoGenerateColumns="False"
                              ItemsSource="{Binding Transactions}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Место действия" Binding="{Binding window_name}" 
                                                Width="auto"/>
                            <DataGridTextColumn Header="Тип операции" Binding="{Binding type_operation.type_operation_name}" 
                                                Width="auto"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding date}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="ФИО Сотрудника" Binding="{Binding employees.EmployeeFullName}" 
                                                Width="*"/>
                            <!-- Новое поле -->
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>
    </Border>

</Window>
